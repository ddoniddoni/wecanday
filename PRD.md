# WeCanDay 제품 요구사항 문서 (PRD)

- 문서 상태: Draft v1.0
- 작성일: 2026-07-12
- 제품명: **WeCanDay** (가칭, 한국어 표기: 위캔데이)
- 대상 플랫폼: iOS, Android
- 주요 독자: 제품 기획자, 디자이너, Codex 및 개발 에이전트, QA
- 연관 문서: `AGENTS.md`

---

## 1. 문서 목적

이 문서는 WeCanDay의 제품 의도, 사용자 흐름, 기능 범위, 비즈니스 규칙, 데이터 규칙 및 출시 기준을 정의한다. 개발 에이전트는 기능을 임의로 해석하지 말고 본 문서를 제품 동작의 기준으로 사용한다.

이 문서에서 아직 확정되지 않은 가격, 최종 브랜드 에셋, 출시 언어 수는 설정값으로 격리한다. 이 항목들이 미정이라는 이유로 핵심 개발을 중단하지 않는다.

---

## 2. 제품 한 문장

> 사용자가 자신만의 하루 시작 시간을 정하고, 장·단기 계획을 매일의 작은 행동으로 이어가며, 친구와 함께 스트릭을 쌓고 픽셀 캐릭터를 성장시키는 글로벌 목표 완주 앱.

---

## 3. 문제 정의

일반적인 습관 앱은 자정에 하루가 초기화되고, 모든 사용자가 비슷한 생활 리듬을 가진다고 가정한다. 그러나 교대 근무자, 야간 학습자, 프리랜서, 시차가 있는 사용자처럼 실제 생활 리듬은 사람마다 다르다.

또한 많은 목표 관리 앱은 계획 작성에는 강하지만 사용자가 실제로 매일 행동하도록 만드는 동기, 사회적 책임감, 시각적 보상은 약하다. WeCanDay는 다음 문제를 해결한다.

1. 사용자의 하루가 반드시 00:00에 시작하지 않는 문제
2. 장기 목표가 너무 커서 오늘 무엇을 해야 할지 모르는 문제
3. 며칠 지나면 동기가 약해져 기록이 끊기는 문제
4. 친구와 함께하고 싶지만 전화번호나 이메일을 공개하고 싶지 않은 문제
5. 목표 관리 앱이 지나치게 사무적이고 재미가 없는 문제

---

## 4. 핵심 제품 원칙

### 4.1 행동이 계획보다 우선한다

사용자가 앱을 열었을 때 가장 먼저 보는 것은 복잡한 프로젝트 보드가 아니라 **오늘 해야 할 행동과 완료 버튼**이다.

### 4.2 동기를 주되 죄책감을 주지 않는다

알림과 캐릭터 반응은 친근해야 한다. 실패, 미완료, 스트릭 종료를 수치심이나 공포로 표현하지 않는다. 캐릭터는 죽거나 영구적으로 손상되지 않는다.

### 4.3 하루의 경계는 사용자가 정한다

사용자는 자신의 현지 시간대를 기준으로 하루 시작 시각을 설정한다. 앱의 체크인, 통계, 스트릭, 알림은 이 설정을 공통으로 사용한다.

### 4.4 세계 어디서나 사용할 수 있어야 한다

국가와 언어는 별도 값이다. 날짜, 시간, 숫자, 주 시작 요일, 텍스트 방향을 지역에 맞게 처리하며 특정 국가의 문화나 시간대에 종속된 로직을 만들지 않는다.

### 4.5 소셜 기능은 비공개가 기본이다

전화번호부 업로드나 이메일 검색을 사용하지 않는다. 사용자가 공유한 12자리 고유 코드로만 친구 요청을 보낼 수 있으며, 공개되는 정보는 최소화한다.

### 4.6 게임처럼 즐겁지만 도구로서 신뢰할 수 있어야 한다

픽셀 캐릭터, 성장, 테마, 애니메이션을 제공하되 기록의 정확성, 접근성, 동기화, 데이터 보호를 희생하지 않는다.

---

## 5. 목표 사용자

### 5.1 핵심 사용자

- 30일, 100일, 1년 등 일정 기간 목표를 완주하고 싶은 사람
- 취업 준비, 공부, 운동, 창작, 독서, 사이드 프로젝트를 매일 이어가고 싶은 사람
- 자정 이후에 활동하거나 교대 근무를 하여 일반적인 날짜 기준이 맞지 않는 사람
- 친구, 연인, 동료와 함께 서로의 진행 상황을 확인하고 싶은 사람
- 딱딱한 생산성 앱보다 캐릭터와 게임 보상을 선호하는 사람

### 5.2 대표 사용 예시

- “60일 동안 매일 취업 프로젝트를 1시간 개발하기”
- “100일 동안 영어 단어 20개 복습하기”
- “한 달 동안 주 5회 30분 걷기”
- “친구와 30일 동안 매일 물 한 병 더 마시기”
- “야간 근무가 끝난 오전 5시를 나의 하루 종료 시각으로 사용하기”

---

## 6. 제품 용어

| 용어 | 정의 |
|---|---|
| 계획(Plan) | 시작일과 선택적 종료일이 있는 장·단기 목표 묶음 |
| 루틴 항목(Routine Item) | 계획을 위해 특정 요일마다 수행하는 오늘의 체크 항목 |
| 루틴 데이(Routine Day) | 사용자가 정한 하루 시작 시각부터 다음 현지 날짜의 같은 시각 직전까지의 논리적 하루 |
| 체크인(Check-in) | 한 루틴 항목을 해당 루틴 데이에 완료했다고 기록하는 행위 |
| 항목 스트릭 | 해당 항목의 예정된 수행일을 연속으로 완료한 횟수 |
| 데일리 스트릭 | 그날 예정된 모든 루틴 항목을 완료한 연속 루틴 데이 수 |
| 동반자(Companion) | 사용자의 완료 행동에 반응하고 성장하는 오리지널 픽셀 캐릭터 |
| 친구(Friend) | 고유 코드 기반 요청을 상호 수락한 사용자 관계 |
| 함께하기(Challenge) | 두 명 이상이 같은 기간과 규칙으로 수행하는 공동 목표 |
| Premium | 무료 제한을 해제하고 추가 테마와 고급 기능을 제공하는 유료 권한 |

---

## 7. 우선순위 정의

- **P0:** 앱 출시와 핵심 가치 구현에 필수. 누락 시 출시 불가.
- **P1:** 첫 정식 버전에 포함하는 것이 바람직. 일정에 따라 베타 후반에 완성 가능.
- **P2:** 첫 출시 이후 검증을 거쳐 추가.

---

## 8. 범위 요약

### 8.1 첫 정식 버전의 P0 범위

- Expo 기반 iOS·Android 앱
- 국가와 언어 선택
- 선택한 언어로 인사 화면 표시
- Google 및 Apple 로그인·회원가입
- 사용자 프로필과 12자리 고유 코드
- 사용자별 시간대 및 하루 시작 시각
- 계획·루틴 항목 생성, 수정, 보관
- 무료 사용자 최대 4개 활성 루틴 항목
- 오늘 목록 체크인, 스트릭, 진행률
- 기본 픽셀 동반자와 완료 애니메이션
- 주간·월간·연간 통계
- 로컬 알림과 스트릭 위험 알림
- 친구 요청, 수락, 차단
- 친구와 함께하는 기본 챌린지
- Premium 월간·연간 구독, 구매 복원
- 테마 시스템: 시스템·라이트·다크·픽셀 테마
- 오프라인 체크인 및 재동기화
- 계정 삭제, 개인정보 및 약관 화면

### 8.2 P1 범위

- 고유 코드 QR 공유와 코드 재발급
- 그룹 챌린지 확장
- 고급 통계 비교 및 데이터 내보내기
- 픽셀 방, 소품, 추가 동반자 상태
- 스트릭 보호권
- 여러 알림 시각
- 친구에게 제한된 응원 보내기

### 8.3 P2 범위

- 사용자가 선택해서 보는 보상형 광고
- 홈 화면 위젯
- AI 기반 계획 분할 제안
- 사진·GitHub·외부 서비스 인증
- 공개 챌린지 탐색
- 웹 대시보드

### 8.4 첫 출시에서 하지 않는 것

- 강제 전면 광고 또는 화면 하단 고정 배너
- 공개 피드, 댓글, 자유로운 1:1 채팅
- 전화번호부 업로드
- 위치 기반 친구 찾기
- 현금성 포인트 및 사용자 간 아이템 거래
- 의료 진단이나 치료 조언
- 복잡한 회사용 프로젝트 관리
- 타마고치, 듀오링고 등 기존 서비스의 캐릭터·에셋·문구를 복제하는 행위

---

## 9. 확정된 기술·제품 결정

| ID | 결정 |
|---|---|
| D-001 | 앱은 웹뷰가 아닌 Expo 기반 React Native로 개발한다. |
| D-002 | 다국어는 `i18next + react-i18next + expo-localization` 조합을 사용한다. |
| D-003 | 백엔드는 Supabase Auth, PostgreSQL, Row Level Security, Edge Functions를 사용한다. |
| D-004 | 소셜 로그인은 Google과 Apple 두 가지를 제공한다. |
| D-005 | 결제 권한 관리는 RevenueCat을 사용하고 실제 결제는 Apple·Google 인앱 결제를 따른다. |
| D-006 | 무료 사용자는 최대 4개의 활성 루틴 항목을 가진다. 보관된 항목과 과거 기록은 제한에 포함하지 않는다. |
| D-007 | 첫 출시에는 강제 광고를 넣지 않는다. 광고 SDK도 기본적으로 비활성화한다. |
| D-008 | 광고를 추가할 경우 사용자가 명시적으로 선택하는 보상형 광고만 우선 검토한다. |
| D-009 | 픽셀 스타일은 오리지널 디자인으로 제작하며, 본문 글꼴은 읽기 쉬운 일반 글꼴을 사용한다. |
| D-010 | 국가, 언어, 시간대는 서로 독립된 설정으로 저장한다. |

---

## 10. 사용자 흐름

### 10.1 최초 실행

1. 앱 시작
2. 국가 선택
3. 언어 선택
4. 선택한 언어로 인사 화면 표시
5. 로그인·회원가입 화면
6. Google 또는 Apple로 계정 생성
7. 표시 이름 확인 또는 입력
8. 시간대 확인
9. 하루 시작 시각 설정
10. 기본 픽셀 동반자 소개
11. 첫 계획과 첫 루틴 항목 생성
12. 첫 루틴 항목을 만든 뒤 알림 권한 요청
13. 홈 화면 진입

한국어 인사 원문은 다음과 같다.

> 안녕하세요, 우리 할 수 있어요

번역 키는 `onboarding.greeting`으로 관리한다. 영어 기본 번역은 `Hello, we can do it.`이다. 번역은 문자열을 코드에서 조합하지 않고 완전한 문장 단위로 제공한다.

### 10.2 재방문

- 인증 세션이 유효하면 즉시 홈으로 이동한다.
- 로그아웃 상태라면 저장된 언어·국가 설정을 유지하고 로그인 화면을 표시한다.
- 사용자가 앱 언어를 변경하면 즉시 UI가 변경되며, 앱 재시작을 요구하지 않는 것을 목표로 한다.

### 10.3 오늘 루틴 수행

1. 홈에서 현재 루틴 데이와 남은 시간을 확인한다.
2. 오늘 예정된 루틴 항목을 확인한다.
3. 항목을 완료하면 체크인한다.
4. 낙관적 UI로 즉시 완료 상태를 보여준다.
5. 서버 동기화에 실패하면 로컬 대기열에 보관하고 재시도 상태를 표시한다.
6. 모든 예정 항목을 완료하면 데일리 스트릭과 동반자 보상을 보여준다.

### 10.4 친구 추가 및 함께하기

1. 프로필에서 자신의 12자리 코드를 복사하거나 공유한다.
2. 상대방이 친구 화면에서 코드를 정확히 입력한다.
3. 상대방은 최소 프로필을 확인하고 친구 요청을 보낸다.
4. 수신자가 수락하면 친구 관계가 생성된다.
5. 친구를 선택하여 챌린지를 만들고 기간·일정을 설정한다.
6. 친구가 수락하면 각자의 오늘 목록에 챌린지 항목이 생성된다.

---

## 11. 상세 기능 요구사항

## 11.1 다국어와 국가 설정

### I18N-001 국가 선택 — P0

- ISO 3166-1 alpha-2 국가 코드를 저장한다.
- 검색 가능한 국가 목록을 제공한다.
- 기기 지역을 추천값으로 보여주되 자동 확정하지 않는다.
- 국가는 언어를 강제로 변경하지 않는다.
- 국기 이모지는 기기별 표현이 다르므로 필수 정보로 사용하지 않는다.

**완료 조건**

- 대한민국을 선택하면 `KR`, 미국을 선택하면 `US`가 저장된다.
- 언어를 영어로 바꾸어도 국가 값은 유지된다.
- 설정 화면에서 언제든 국가를 변경할 수 있다.

### I18N-002 언어 선택 — P0

- 언어 이름은 가능한 한 해당 언어의 자국어 표기로 보여준다. 예: `한국어`, `English`, `日本語`.
- 기기 언어를 추천값으로 사용한다.
- 사용자가 직접 선택한 언어는 기기 언어보다 우선한다.
- 지원하지 않는 언어는 영어로 폴백한다.
- 초기 출시 필수 번역은 한국어와 영어다.
- 일본어, 스페인어, 중국어 간체·번체 등은 번역 완성도 검수 후 설정만으로 활성화할 수 있어야 한다.

**완료 조건**

- 사용자에게 보이는 문자열이 컴포넌트에 하드코딩되지 않는다.
- 날짜·숫자·복수형이 선택 언어에 맞게 출력된다.
- 번역 누락 시 키 문자열이 그대로 노출되지 않고 영어 폴백 또는 안전한 기본 문구가 표시된다.

### I18N-003 RTL 준비 — P1

- `left`, `right`에 의존하는 레이아웃보다 논리적 시작·끝 개념을 사용한다.
- 아이콘 방향이 의미를 가지는 경우 RTL에서 반전 여부를 명시한다.
- 아랍어 등 RTL 언어 활성화 전 자동화 테스트와 수동 검수를 수행한다.

---

## 11.2 인증과 계정

### AUTH-001 Google 로그인·회원가입 — P0

- iOS와 Android 모두에서 제공한다.
- 신규 사용자는 인증 성공 후 프로필 생성 단계로 이동한다.
- 기존 사용자는 동일 계정으로 로그인한다.
- 사용자가 로그인 창을 취소한 경우 오류처럼 취급하지 않는다.

### AUTH-002 Apple 로그인·회원가입 — P0

- iOS에서는 네이티브 Sign in with Apple을 사용한다.
- Android에서는 Apple OAuth 브라우저 흐름을 제공한다.
- nonce, PKCE, 딥링크, 토큰 검증을 올바르게 처리한다.
- Apple이 이름을 최초 한 번만 제공할 수 있으므로 최초 응답을 안전하게 저장한다.

### AUTH-003 계정 연결 — P1

- 동일한 검증 이메일을 사용하는 Google·Apple 계정은 백엔드의 안전한 identity linking 규칙을 따른다.
- Apple 비공개 이메일로 자동 연결할 수 없는 경우 사용자가 명시적으로 계정을 연결할 수 있는 흐름을 제공한다.
- 서로 다른 계정의 기록을 자동 병합하지 않는다.

### AUTH-004 로그아웃 — P0

- Supabase 세션, RevenueCat 사용자 상태, 로컬 민감 캐시를 정리한다.
- 언어와 국가처럼 로그인 이전에도 필요한 비민감 설정은 유지할 수 있다.

### AUTH-005 계정 삭제 — P0

- 앱 설정 안에서 사용자가 삭제를 시작할 수 있어야 한다.
- 재인증 또는 명확한 확인 절차를 제공한다.
- 사용자 데이터, 친구 관계, 토큰, 루틴, 체크인, 챌린지 멤버십을 삭제 또는 익명화한다.
- 법적·보안상 보관해야 하는 데이터가 있다면 사용자에게 고지한다.
- Sign in with Apple 사용자의 경우 필요한 토큰 해지 절차를 포함한다.

---

## 11.3 프로필과 고유 코드

### PROF-001 기본 프로필 — P0

프로필은 다음 항목을 가진다.

- 표시 이름: 1~30자
- 선택 국가
- 선택 언어
- 픽셀 아바타 또는 동반자 미리보기
- 12자리 고유 코드
- 현재 테마
- 가입일

이메일 주소는 친구에게 공개하지 않는다.

### PROF-002 12자리 고유 코드 — P0

- 문자 집합: `A-Z`, `a-z`, `0-9`
- 길이: 정확히 12자
- 정규식: `^[A-Za-z0-9]{12}$`
- 서버에서 암호학적으로 안전한 난수로 생성한다.
- 데이터베이스 고유 제약조건을 둔다.
- 충돌 시 서버에서 재생성한다.
- 대소문자를 구분한다.
- 코드 검색은 정확 일치만 허용한다.
- 코드를 모른 채 사용자를 탐색할 수 있는 자동완성이나 부분 검색은 제공하지 않는다.

예시 형식:

```text
Ab7kL2xP9Qm4
```

### PROF-003 코드 공유 — P0

- 복사 버튼
- 시스템 공유 시트
- 코드 입력 안내
- 상대방에게 공개되는 최소 프로필 미리보기

### PROF-004 코드 재발급 — P1

- 코드가 유출되었을 때 사용자가 재발급할 수 있다.
- 재발급 즉시 이전 코드는 무효가 된다.
- 오발급 방지를 위해 확인 단계를 제공한다.
- 남용 방지를 위해 재발급 빈도를 제한한다.

---

## 11.4 사용자별 하루 기준

### DAY-001 시간대 — P0

- 기기의 IANA 시간대 값을 추천한다. 예: `Asia/Seoul`, `America/New_York`.
- 국가는 시간대를 결정하지 않는다.
- 사용자가 여행 중일 때 다음 중 선택할 수 있어야 한다.
  - 기존 시간대 유지
  - 새 기기 시간대로 변경
- 시간대 변경은 사용자의 명시적 확인 없이 자동 적용하지 않는다.

### DAY-002 하루 시작 시각 — P0

- 사용자는 00:00~23:59 사이에서 하루 시작 시각을 선택한다.
- 분 단위로 저장하되 UI는 5분 또는 15분 간격 선택을 제공할 수 있다.
- 예: 하루 시작 시각이 04:00이면 2026-07-12 03:59는 2026-07-11 루틴 데이에 속하고, 04:00부터 2026-07-12 루틴 데이에 속한다.
- 12시간, 36시간 같은 사용자 정의 주기는 지원하지 않는다. 하루는 현지 날짜마다 한 번만 전환되는 논리적 하루다.

### DAY-003 루틴 데이 계산 — P0

표준 계산 규칙:

```text
localNow = 현재 UTC 시각을 사용자 IANA 시간대로 변환
if localNow의 시각 < dayStart:
  routineDay = localNow의 현지 날짜 - 1일
else:
  routineDay = localNow의 현지 날짜
```

루틴 데이 구간은 해당 `routineDay`의 `dayStart`부터 다음 현지 날짜의 `dayStart` 직전까지다.

**DST 주의**

전 세계 사용을 위해 현지 시작 시각을 고정한다. 대부분의 날은 실제 24시간이지만 서머타임 전환 지역에서는 한 번의 구간이 실제 경과시간 기준 23시간 또는 25시간일 수 있다. 제품에서는 이를 한 번의 정상적인 “루틴 데이”로 취급한다. 현지 시각 고정과 정확히 24시간 경과를 동시에 보장할 수 없으므로, WeCanDay는 사용자가 선택한 현지 시각 고정을 우선한다.

### DAY-004 설정 변경 — P0

- 하루 시작 시각 변경은 현재 루틴 데이가 끝난 다음 구간부터 적용한다.
- 과거 체크인의 `routine_day`는 재계산하지 않는다.
- 한 사용자가 짧은 시간 안에 반복 변경하여 스트릭을 조작하지 못하도록 서버 감사 로그와 빈도 제한을 둔다.
- 활성 챌린지 중 변경된 시간대·시작 시각은 해당 사용자에게만 적용되지만, 과거 순위는 변경하지 않는다.

---

## 11.5 계획과 루틴 항목

### PLAN-001 계획 생성 — P0

계획 필드:

- 제목: 필수, 1~80자
- 설명: 선택, 최대 500자
- 시작일: 필수
- 종료일: 선택
- 아이콘 또는 픽셀 심볼
- 테마 색상 토큰
- 공개 범위: 나만 보기 / 친구에게 진행률 공개
- 상태: 초안 / 활성 / 일시정지 / 완료 / 보관

### PLAN-002 루틴 항목 생성 — P0

루틴 항목 필드:

- 연결된 계획
- 행동 제목: 필수, 1~80자
- 선택 요일 또는 매일
- 시작일·종료일은 계획 범위 안에서 상속
- 선택적 알림 시각
- 정렬 순서
- 챌린지 연결 여부

첫 출시의 완료 기준은 이진 상태다.

```text
미완료 / 완료
```

시간, 횟수, 페이지 수 같은 수량형 기록은 P1 이후로 둔다.

### PLAN-003 무료 제한 — P0

- 무료 사용자는 총 4개의 활성 루틴 항목을 가질 수 있다.
- `일시정지`, `완료`, `보관` 상태는 제한에 포함하지 않는다.
- 챌린지를 오늘 목록에 추가한 항목도 활성 루틴 수에 포함한다.
- 5번째 항목을 활성화하려 할 때 기존 항목 보관 또는 Premium 가입을 안내한다.
- 과거 기록을 삭제하도록 강요하지 않는다.
- UI뿐 아니라 서버에서도 제한을 검증한다.

### PLAN-004 Premium 제한 해제 — P0

- Premium 사용자는 4개를 초과하여 활성화할 수 있다.
- 일반 사용 범위에서는 무제한으로 표현할 수 있으나, 비정상 사용 방지를 위한 높은 서버 안전 한도는 둘 수 있다.
- 구독 만료 시 기존 5개 이상의 항목을 삭제하지 않는다.
- 만료 후에는 사용자가 4개 이하로 줄이기 전까지 새 항목 생성·재활성화만 제한한다.
- 기존 항목의 체크인과 기록 열람은 계속 허용한다.

### PLAN-005 일시정지·보관 — P0

- 일시정지한 항목은 예정 목록과 스트릭 판정에서 제외한다.
- 보관해도 기록과 통계는 유지한다.
- 계획 종료일 이후 자동으로 완료 제안을 보여주되 임의 삭제하지 않는다.

---

## 11.6 체크인과 스트릭

### CHECK-001 오늘 체크인 — P0

- 오늘 예정된 항목만 기본 홈에 표시한다.
- 한 항목은 한 루틴 데이에 최대 한 번 완료할 수 있다.
- 동일 체크인 요청이 반복되어도 중복 행을 만들지 않는다.
- 루틴 데이가 끝나기 전에는 완료를 취소할 수 있다.

### CHECK-002 오프라인 체크인 — P0

- 네트워크가 없어도 체크 상태가 즉시 반영된다.
- 체크인은 로컬 SQLite 대기열에 저장된다.
- 각 요청은 고유 idempotency key를 가진다.
- 연결 복구 후 서버와 자동 동기화한다.
- 동기화 실패가 계속되면 사용자가 상태를 알 수 있어야 한다.

### CHECK-003 항목 스트릭 — P0

- 항목이 예정된 루틴 데이에 완료되면 1 증가한다.
- 예정되지 않은 날은 증가하지 않고 끊기지도 않는다.
- 예정된 날에 완료하지 않으면 해당 항목 스트릭이 종료된다.
- 일시정지가 미리 적용된 날은 예정일로 계산하지 않는다.

### CHECK-004 데일리 스트릭 — P0

- 해당 루틴 데이에 예정 항목이 1개 이상 있어야 한다.
- 예정된 모든 항목을 완료하면 데일리 스트릭이 1 증가한다.
- 예정 항목이 없는 날은 증가하거나 끊기지 않는다.
- 홈의 대표 불꽃 아이콘은 데일리 스트릭을 표시한다.
- 항목 상세에서는 항목별 스트릭을 표시한다.

### CHECK-005 과거 기록 수정 — P1

- 개인 항목은 제한된 기간 안에서 사용자가 누락을 수정할 수 있다.
- 공동 챌린지 기록은 공정성을 위해 루틴 데이 종료 후 수정 제한을 더 엄격히 적용한다.
- 수정 시 감사 필드를 남긴다.

### CHECK-006 스트릭 보호권 — P1

- 사용자가 하루를 놓쳤을 때 조건을 만족하면 자동 사용한다.
- 실제 완료율 통계는 변경하지 않고 스트릭 표시만 보호한다.
- 보상형 광고 또는 Premium 보상으로 제공할 수 있다.
- 공동 챌린지 순위에는 보호권을 실제 완료로 계산하지 않는다.

---

## 11.7 홈과 픽셀 게임 경험

### GAME-001 홈 화면 — P0

홈은 다음 정보를 우선순위대로 보여준다.

1. 픽셀 동반자와 현재 상태
2. 선택한 하루 시작 시각 기준의 현재 루틴 데이
3. 루틴 데이 종료까지 남은 시간
4. 데일리 스트릭
5. 오늘의 루틴 항목
6. 오늘 완료율
7. 짧고 친근한 응원 문구

### GAME-002 픽셀 동반자 — P0

- 오리지널 캐릭터를 사용한다.
- 최소 상태: 기본, 기쁨, 응원, 휴식, 모두 완료
- 루틴 완료 시 짧은 애니메이션과 선택적 햅틱을 제공한다.
- 완료하지 않았다는 이유로 캐릭터가 죽거나 병드는 표현은 사용하지 않는다.
- 사용자가 `동작 줄이기`를 켜면 애니메이션을 단순화한다.

### GAME-003 경험치와 성장 — P1

- 완료 시 경험치를 제공한다.
- 레벨은 꾸준함을 시각화하지만 친구 경쟁의 절대 평가로 사용하지 않는다.
- 경험치 계산은 서버 또는 검증 가능한 공통 규칙을 사용한다.
- 유료 결제로 직접 스트릭이나 실제 완료율을 구매하게 하지 않는다.

### GAME-004 테마 — P0

기본 제공:

- 시스템 테마
- 라이트
- 다크
- 기본 픽셀 테마 1종

Premium 후보:

- 파스텔 픽셀
- 우주
- 숲
- 새벽
- 레트로 게임룸

모든 테마는 동일한 의미의 디자인 토큰을 사용한다. 상태를 색상만으로 구분하지 않는다.

### GAME-005 픽셀 디자인 원칙 — P0

- 픽셀 에셋은 정수 배율로 확대하여 흐려지지 않도록 한다.
- 작은 본문까지 픽셀 폰트를 강제하지 않는다.
- 본문, 설정, 통계는 가독성 높은 시스템 또는 라이선스가 확인된 일반 글꼴을 사용한다.
- 픽셀 폰트는 로고, 제목, 배지처럼 제한된 장식 영역에 사용한다.

---

## 11.8 통계

### STAT-001 공통 계산 — P0

```text
완료율 = 완료한 예정 항목 수 / 전체 예정 항목 수 × 100
```

- 예정 항목이 0개인 구간은 완료율 평균의 분모에서 제외한다.
- 통계는 사용자의 루틴 데이를 기준으로 집계한다.
- 과거 시간대 변경이 과거 루틴 데이를 재분류하지 않는다.

### STAT-002 주간 통계 — P0

- 주간 완료율
- 일별 완료 막대 또는 픽셀 셀
- 현재·최고 데일리 스트릭
- 가장 꾸준한 항목
- 주 시작 요일은 지역 기본값을 추천하고 사용자가 변경할 수 있다.

### STAT-003 월간 통계 — P0

- 월간 캘린더 히트맵
- 완료한 루틴 데이 수
- 계획별 완료율
- 지난달 대비 변화

### STAT-004 연간 통계 — P0

- 12개월 히트맵
- 총 체크인 수
- 최고 연속 기록
- 월별 추세
- 연도 선택

### STAT-005 고급 통계 — P1 / Premium

- 사용자 지정 기간
- 요일별 성공 패턴
- 시간대·알림과 완료율의 상관 표시
- CSV 또는 JSON 내보내기
- 이전 기간 상세 비교

기본 주간·월간·연간 통계는 무료 사용자에게도 제공한다. Premium은 분석 깊이와 내보내기를 확장한다.

---

## 11.9 친구와 함께하기

### SOCIAL-001 코드로 친구 찾기 — P0

- 정확한 12자리 코드를 입력한다.
- 자기 자신은 검색 결과에 나오지 않는다.
- 차단 관계는 결과에 나오지 않는다.
- 결과에는 표시 이름, 픽셀 아바타, 사용자가 공개한 최소 정보만 보인다.
- 무차별 코드 대입을 막기 위해 속도 제한과 비정상 탐지 규칙을 둔다.

### SOCIAL-002 친구 요청 — P0

상태:

```text
pending / accepted / declined / cancelled / removed
```

- 중복 요청을 만들지 않는다.
- 상대가 이미 요청했다면 하나의 관계로 합리적으로 처리한다.
- 사용자는 보낸 요청을 취소할 수 있다.
- 수신자는 수락 또는 거절할 수 있다.

### SOCIAL-003 친구 공개 범위 — P0

기본 공개:

- 표시 이름
- 픽셀 아바타
- 친구 여부

선택 공개:

- 현재 데일리 스트릭
- 오늘 완료 여부
- 챌린지 진행률

비공개:

- 이메일
- 국가의 상세 주소
- 원문 메모
- 전체 루틴 제목, 사용자가 공개하지 않은 계획
- 구독 상태

### SOCIAL-004 친구 삭제·차단·신고 — P0

- 친구 삭제는 양쪽 관계를 해제한다.
- 차단하면 검색, 요청, 초대, 응원 알림을 막는다.
- 신고는 최소한의 사유 코드와 선택적 설명을 받는다.
- 신고했다고 상대에게 알리지 않는다.

### SOCIAL-005 함께하는 챌린지 — P0

필드:

- 제목
- 설명
- 시작일·종료일
- 수행 요일
- 초대한 사용자
- 참가자별 공개 범위
- 상태: 초대 / 활성 / 완료 / 취소

규칙:

- 챌린지는 각 참가자의 시간대와 하루 시작 시각을 따른다.
- 한 사용자의 “오늘”과 다른 사용자의 “오늘”이 UTC 기준으로 다를 수 있다.
- 진행률은 각 참가자의 예정 수행일 대비 완료율로 비교한다.
- 친구의 상세 메모는 공유하지 않는다.
- 챌린지 항목은 무료 4개 활성 항목 제한에 포함된다.

### SOCIAL-006 응원 — P1

- 자유 입력 채팅 대신 미리 정의된 응원 이모션 또는 번역 가능한 짧은 메시지를 사용한다.
- 일일 전송 횟수를 제한한다.
- 수신자가 응원 알림을 끌 수 있다.

---

## 11.10 알림

### NOTIF-001 권한 요청 — P0

- 첫 실행 직후 바로 요청하지 않는다.
- 첫 루틴 항목을 만든 뒤 알림의 가치를 설명하고 요청한다.
- 거절해도 앱 핵심 기능을 계속 사용할 수 있다.

### NOTIF-002 루틴 알림 — P0

- 항목별 선택 알림 시각
- 선택 언어로 현지화
- 알림을 누르면 해당 루틴 항목 또는 오늘 화면으로 딥링크
- 완료한 항목의 남은 알림은 취소 또는 무시

### NOTIF-003 스트릭 위험 알림 — P0

- 루틴 데이 종료 전 아직 미완료 항목이 있을 때 선택적으로 보낸다.
- 기본 추천 시각은 종료 2시간 전이며 사용자가 변경하거나 끌 수 있다.
- 하루에 같은 이유로 반복 발송하지 않는다.
- 비난하는 표현을 사용하지 않는다.

예시 톤:

```text
오늘의 작은 한 걸음이 남아 있어요.
지금 하나만 완료해도 흐름을 이어갈 수 있어요.
```

### NOTIF-004 소셜 푸시 — P0

- 친구 요청
- 챌린지 초대
- 챌린지 수락
- 선택적 응원

잠금 화면에 민감한 계획 제목이나 메모를 기본 노출하지 않는다.

### NOTIF-005 재예약 — P0

다음 설정이 바뀌면 로컬 알림을 다시 계산한다.

- 언어
- 시간대
- 하루 시작 시각
- 루틴 요일
- 알림 시각
- 루틴 활성 상태

---

## 11.11 Premium과 결제

### BILL-001 요금제 — P0

최소 요금제:

- 월간 Premium
- 연간 Premium

가격은 App Store Connect와 Google Play Console에서 지역별로 관리하며 앱에 숫자를 하드코딩하지 않는다.

### BILL-002 Premium 권한 — P0

Premium 혜택:

- 4개 초과 활성 루틴 항목
- 추가 픽셀 테마와 꾸미기
- 고급 통계 및 데이터 내보내기
- 더 많은 동시 챌린지
- 향후 광고가 도입될 경우 광고 없음
- 향후 스트릭 보호권 정기 제공

친구 추가, 기본 챌린지, 기본 통계는 무료에도 제공하여 네트워크 효과를 막지 않는다.

### BILL-003 구매 흐름 — P0

- RevenueCat Offering에서 상품과 지역 가격을 읽는다.
- 구매 성공 후 entitlement를 즉시 갱신한다.
- 구매 취소는 오류 토스트로 과도하게 표시하지 않는다.
- 네트워크 오류와 실제 결제 실패를 구분한다.
- 구매 복원 버튼을 설정과 페이월에 제공한다.
- 현재 구독 상태와 관리 경로를 보여준다.

### BILL-004 서버 권한 검증 — P0

- 클라이언트 표시만으로 무료 제한을 해제하지 않는다.
- RevenueCat 웹훅을 검증하여 서버 entitlement를 갱신한다.
- 활성 루틴 수 제한은 서버 권한을 기준으로 검사한다.
- 만료, 환불, 유예 기간, 해지 후 만료를 처리한다.

---

## 11.12 광고에 대한 제품 결정

### ADS-001 첫 출시 — P0

**첫 출시에는 광고를 넣지 않는다.**

이유:

- 무료 4개 제한 자체가 명확한 Premium 전환 장치다.
- 목표 체크 흐름에 배너나 전면 광고가 들어가면 집중과 신뢰를 해친다.
- 초기 사용자 수에서는 광고 수익보다 SDK 복잡성, 개인정보 동의, 심사 부담이 더 클 가능성이 높다.
- 픽셀 동반자의 친근한 브랜드와 강제 광고의 인상이 충돌한다.

코드에는 다음 기능 플래그를 둘 수 있으나 기본값은 꺼짐이다.

```text
ads_enabled = false
rewarded_ads_enabled = false
```

### ADS-002 추후 실험 — P2

광고를 실험한다면 다음 원칙을 적용한다.

- 사용자가 버튼을 눌러 선택하는 보상형 광고만 우선 사용
- 자동 전면 광고 금지
- 오늘 체크 완료 직전·직후 강제 광고 금지
- 화면 하단 상시 배너 금지
- 보상은 현금화·양도할 수 없는 소품, 일시적 꾸미기, 제한된 보호권으로 제한
- Premium 사용자는 광고 없이 동등하거나 더 나은 혜택 제공
- 개인정보 동의와 지역 규정을 충족하기 전 광고 SDK 초기화 금지
- 챌린지 실제 완료율이나 순위를 광고 보상으로 올리지 않음

---

## 11.13 설정과 개인정보

### SET-001 설정 화면 — P0

- 프로필 편집
- 국가
- 언어
- 시간대
- 하루 시작 시각
- 주 시작 요일
- 테마
- 동작 줄이기
- 햅틱
- 알림
- 친구 공개 범위
- 구독 상태 및 구매 복원
- 개인정보처리방침
- 이용약관
- 데이터 내보내기(P1)
- 로그아웃
- 계정 삭제

### SET-002 연령과 이용 자격 — P0

- 기본 출시 대상은 만 13세 이상으로 설정한다.
- 실제 출시 국가의 요구에 따라 더 높은 연령 기준이 필요할 수 있으므로 약관과 온보딩 문구를 설정 가능하게 한다.
- 생년월일은 핵심 기능에 불필요하면 수집하지 않는다.

### SET-003 데이터 최소화 — P0

- 친구 기능을 위해 전화번호나 연락처를 요구하지 않는다.
- 소셜 로그인에서 필요한 최소 프로필만 저장한다.
- 분석 이벤트에 계획 제목, 메모, 이메일, 공개 코드를 넣지 않는다.
- 알림 본문에 민감 정보를 기본으로 넣지 않는다.

---

## 11.14 접근성

### A11Y-001 기본 접근성 — P0

- 모든 터치 요소에 접근성 역할과 라벨을 제공한다.
- 최소 터치 영역은 44×44pt 수준을 목표로 한다.
- 텍스트 확대에 대응한다.
- 명도 대비를 확보한다.
- 완료 상태를 색상만으로 전달하지 않는다.
- 스크린 리더 읽기 순서가 시각적 순서와 일치한다.
- 시스템 `동작 줄이기` 설정과 앱 설정을 존중한다.
- 햅틱과 소리는 끌 수 있다.

### A11Y-002 픽셀 UI 접근성 — P0

- 픽셀 장식이 텍스트 가독성을 방해하지 않는다.
- 통계 그래프는 접근성 요약 텍스트를 함께 제공한다.
- 캐릭터 감정 상태는 이미지 외에 접근성 설명을 제공한다.

---

## 12. 정보 구조와 주요 화면

```text
앱 시작
├─ 온보딩
│  ├─ 국가 선택
│  ├─ 언어 선택
│  ├─ 인사 화면
│  └─ 로그인
├─ 초기 설정
│  ├─ 표시 이름
│  ├─ 시간대
│  ├─ 하루 시작 시각
│  └─ 첫 계획 만들기
└─ 메인 탭
   ├─ 오늘
   │  ├─ 픽셀 동반자
   │  ├─ 데일리 스트릭
   │  └─ 오늘 루틴
   ├─ 계획
   │  ├─ 계획 목록
   │  ├─ 계획 상세
   │  └─ 계획/루틴 편집
   ├─ 통계
   │  ├─ 주간
   │  ├─ 월간
   │  └─ 연간
   ├─ 친구
   │  ├─ 코드 검색
   │  ├─ 요청함
   │  ├─ 친구 목록
   │  └─ 챌린지
   └─ 프로필
      ├─ 고유 코드
      ├─ 테마
      ├─ Premium
      └─ 설정
```

---

## 13. 권장 기술 구조

### 13.1 클라이언트

- Expo + React Native + TypeScript
- Expo Router
- TanStack Query: 서버 상태
- Zustand: 짧은 UI 상태와 생성 중 임시 상태
- React Hook Form + Zod: 입력과 검증
- `i18next + react-i18next + expo-localization`: 다국어
- `expo-notifications`: 로컬·푸시 알림
- `expo-secure-store`: 세션 관련 작은 민감 데이터
- `expo-sqlite`: 오프라인 캐시와 동기화 대기열
- `react-native-reanimated`: 픽셀 동반자 애니메이션
- RevenueCat React Native SDK: 구독 권한
- `date-fns + @date-fns/tz`: 시간대 및 루틴 데이 계산

버전은 문서에 고정하지 않고 설치 시점의 Expo SDK 호환 버전을 사용한다. Expo 모듈은 가능한 한 `npx expo install`로 설치한다.

### 13.2 백엔드

- Supabase Auth
- PostgreSQL
- Row Level Security
- Database Functions / RPC
- Edge Functions
- Supabase Realtime은 필요한 소셜 상태에만 제한적으로 사용
- Expo Push Service 또는 추후 직접 FCM/APNs

### 13.3 배포

- EAS Build
- EAS Submit
- EAS Update는 스토어 정책과 런타임 버전 호환 범위 안에서 사용
- TestFlight
- Google Play Internal Testing

---

## 14. 데이터 모델 초안

실제 마이그레이션은 별도 SQL로 작성하되, 아래 의미와 제약을 유지한다.

### 14.1 `profiles`

| 필드 | 타입 예시 | 규칙 |
|---|---|---|
| `id` | uuid | `auth.users.id` 참조, PK |
| `display_name` | varchar(30) | 필수 |
| `country_code` | char(2) | ISO 국가 코드 |
| `locale` | varchar(16) | BCP 47 형식 |
| `public_code` | varchar(12) | 고유, 대소문자 구분 |
| `avatar_seed` | text | 픽셀 아바타 표현 |
| `created_at` | timestamptz | 서버 생성 |
| `updated_at` | timestamptz | 서버 갱신 |

### 14.2 `user_settings`

| 필드 | 타입 예시 | 규칙 |
|---|---|---|
| `user_id` | uuid | PK, profile 참조 |
| `time_zone` | text | 유효한 IANA 시간대 |
| `routine_day_start_minute` | smallint | 0~1439 |
| `week_starts_on` | smallint | 0~6 |
| `theme_id` | text | 테마 레지스트리 키 |
| `reduce_motion` | boolean | 기본 시스템 설정 |
| `haptics_enabled` | boolean | 사용자 설정 |
| `streak_warning_minutes` | integer | 기본 120, null이면 꺼짐 |
| `timezone_change_pending` | jsonb | 다음 루틴 데이에 적용할 변경 |

### 14.3 `plans`

| 필드 | 타입 예시 | 규칙 |
|---|---|---|
| `id` | uuid | PK |
| `owner_id` | uuid | 사용자 |
| `title` | varchar(80) | 필수 |
| `description` | varchar(500) | 선택 |
| `start_date` | date | 필수 |
| `end_date` | date | 선택, 시작일 이상 |
| `status` | enum/text | draft, active, paused, completed, archived |
| `visibility` | enum/text | private, friends_progress |
| `icon_key` | text | 정적 아이콘 키 |
| `color_token` | text | 테마 토큰 |

### 14.4 `routine_items`

| 필드 | 타입 예시 | 규칙 |
|---|---|---|
| `id` | uuid | PK |
| `plan_id` | uuid | 계획 참조 |
| `owner_id` | uuid | 중복 저장 시 일관성 제약 필요 |
| `title` | varchar(80) | 필수 |
| `weekdays` | smallint[] | 0~6, 매일은 7개 |
| `reminder_local_time` | time | 선택 |
| `position` | integer | 정렬 |
| `status` | enum/text | active, paused, completed, archived |
| `challenge_id` | uuid | 선택 |
| `created_at` | timestamptz | 서버 생성 |

### 14.5 `check_ins`

| 필드 | 타입 예시 | 규칙 |
|---|---|---|
| `id` | uuid | PK |
| `user_id` | uuid | 소유자 |
| `routine_item_id` | uuid | 항목 참조 |
| `routine_day` | date | 사용자 논리적 날짜 |
| `completed_at` | timestamptz | 실제 완료 시각 |
| `received_at` | timestamptz | 서버 수신 시각 |
| `source` | text | online, offline_sync, correction |
| `idempotency_key` | uuid | 고유 |

필수 고유 제약:

```text
unique(user_id, routine_item_id, routine_day)
unique(idempotency_key)
```

### 14.6 `friendships`

- `requester_id`
- `addressee_id`
- `status`
- `created_at`
- `responded_at`

사용자 쌍의 중복 관계를 막는 고유 제약이 필요하다.

### 14.7 `user_blocks`

- `blocker_id`
- `blocked_id`
- `created_at`

### 14.8 `challenges`

- `id`
- `creator_id`
- `title`
- `description`
- `start_date`
- `end_date`
- `weekdays`
- `status`
- `created_at`

### 14.9 `challenge_members`

- `challenge_id`
- `user_id`
- `status`: invited, accepted, declined, left
- `linked_routine_item_id`
- `joined_at`

### 14.10 `device_push_tokens`

- `user_id`
- `expo_push_token`
- `platform`
- `device_id_hash`
- `locale`
- `enabled`
- `last_seen_at`

### 14.11 `entitlements`

- `user_id`
- `entitlement_key`: `premium`
- `is_active`
- `source`: app_store, play_store, promotional
- `expires_at`
- `updated_at`

구매 영수증 원문이나 비밀 키를 앱 데이터베이스에 불필요하게 복제하지 않는다.

---

## 15. 서버 동작과 보안 규칙

### 15.1 필수 서버 함수

- 신규 사용자 프로필과 고유 코드 생성
- 정확한 코드로 사용자 최소 프로필 조회
- 친구 요청 생성·응답·취소
- 챌린지 생성·초대·수락
- 체크인 upsert 및 루틴 데이 계산 검증
- 활성 루틴 수 제한 검사
- RevenueCat 웹훅 처리
- 계정 삭제
- 푸시 발송

### 15.2 Row Level Security 원칙

- 모든 사용자 데이터 테이블에 RLS를 켠다.
- 사용자는 자기 프로필 전체만 직접 읽고 수정할 수 있다.
- 친구의 공개 프로필은 제한된 RPC 또는 안전한 뷰로만 제공한다.
- `public_code` 전체 목록에 대한 직접 SELECT를 허용하지 않는다.
- 친구·차단·챌린지 관계를 RLS에서 검증한다.
- 모바일 앱에 service role 키를 절대 포함하지 않는다.
- 서버 함수가 `security definer`를 사용하면 고정 `search_path`와 최소 권한을 적용한다.

### 15.3 고유 코드 보호

- 정확 일치 조회
- 사용자별·IP별 속도 제한
- 존재 여부를 무제한으로 확인할 수 없도록 일반화된 오류 메시지
- 성공한 조회에도 과도한 개인정보를 반환하지 않음
- 코드 재발급 시 이전 코드 즉시 폐기

### 15.4 Premium 제한 보호

- 활성 루틴 항목 생성·활성화 시 서버에서 entitlement를 검사한다.
- 클라이언트의 `isPremium` 값만 신뢰하지 않는다.
- 구독 만료 후 기존 데이터를 삭제하지 않는다.

---

## 16. 오프라인과 동기화

### 16.1 오프라인에서 가능한 작업

- 오늘 목록 보기
- 체크인 및 취소
- 최근 통계 캐시 보기
- 계획 초안 작성

### 16.2 동기화 대기열

각 대기 작업은 다음 값을 가진다.

- 작업 ID
- idempotency key
- 작업 종류
- payload
- 생성 시각
- 재시도 횟수
- 마지막 오류 코드
- 다음 재시도 시각

### 16.3 충돌 규칙

- 체크인은 고유 제약을 이용한 upsert로 병합한다.
- 같은 항목의 같은 루틴 데이에 완료와 취소가 충돌하면 가장 최근의 유효한 사용자 작업과 서버 버전을 비교한다.
- 계획 편집 충돌은 무조건 마지막 쓰기로 덮기보다 `updated_at` 또는 버전 번호로 감지한다.
- 서버 시간이 권한과 만료 판정의 기준이다.

---

## 17. 분석 이벤트

개인정보를 넣지 않는 이벤트 예시:

```text
onboarding_country_selected
onboarding_language_selected
onboarding_completed
login_started
login_succeeded
plan_created
routine_item_created
checkin_completed
checkin_undone
daily_streak_completed
friend_request_sent
challenge_created
paywall_viewed
purchase_started
purchase_succeeded
purchase_restored
account_deletion_requested
```

금지되는 이벤트 속성:

- 이메일
- 표시 이름
- 12자리 공개 코드
- 계획 제목이나 설명
- 친구 코드
- 소셜 인증 토큰
- 정확한 푸시 토큰

분석 SDK 선택은 별도 결정으로 두며, 필수 기능보다 먼저 추가하지 않는다.

---

## 18. 성공 지표

### 18.1 활성화

- 가입 완료율
- 가입 후 첫 계획 생성률
- 가입 후 첫 체크인 완료율
- 첫 루틴 생성까지 걸린 단계 수

### 18.2 유지

- D1, D7, D30 유지율
- 7일 안에 3일 이상 체크인한 사용자 비율
- 평균 활성 루틴 수
- 주간 완료율
- 알림 활성화 사용자와 비활성 사용자 간 유지 차이

### 18.3 소셜

- 고유 코드 공유율
- 친구 요청 수락률
- 친구가 있는 사용자의 유지율
- 챌린지 생성·완주율

### 18.4 수익

- 페이월 조회 대비 구매 전환율
- 월간 대비 연간 선택 비율
- 무료 사용자가 4개 제한에 도달하는 비율
- 구독 유지 및 복원 성공률

### 18.5 품질

- 크래시 없는 세션 비율
- 오프라인 체크인 동기화 성공률
- 알림 딥링크 성공률
- 시간대·DST 관련 오류 건수

---

## 19. 출시 마일스톤

### M0. 기반

- Expo 프로젝트
- TypeScript strict
- Expo Router
- 테마 토큰
- i18n 구조
- Supabase 로컬 개발 및 마이그레이션
- CI: lint, typecheck, unit test

### M1. 온보딩과 인증

- 국가·언어
- 인사 화면
- Google·Apple 인증
- 프로필과 공개 코드
- 하루 시작 시각
- 계정 삭제 골격

### M2. 핵심 루틴

- 계획·루틴 CRUD
- 무료 4개 제한
- 루틴 데이 계산
- 오늘 목록
- 체크인·오프라인 동기화
- 스트릭
- 로컬 알림

### M3. 게임과 통계

- 픽셀 동반자 기본 상태
- 완료 애니메이션
- 라이트·다크·픽셀 테마
- 주간·월간·연간 통계

### M4. 소셜

- 코드 검색
- 친구 요청·차단
- 기본 챌린지
- 소셜 푸시

### M5. 결제와 출시 준비

- RevenueCat
- 월간·연간 Premium
- 구매 복원
- 서버 entitlement
- 접근성 검수
- 계정 삭제 완성
- 개인정보·약관
- TestFlight / Play Internal Test
- 스토어 메타데이터 및 심사 계정

---

## 20. 핵심 수용 테스트

### AT-001 언어와 인사

- 새 설치에서 국가 `KR`, 언어 `ko`를 선택한다.
- 인사 화면에 `안녕하세요, 우리 할 수 있어요`가 표시된다.
- 설정에서 `en`으로 변경하면 즉시 영어 문구가 표시된다.

### AT-002 사용자 코드

- 신규 계정을 생성한다.
- 프로필에 `[A-Za-z0-9]{12}` 형식의 코드가 보인다.
- 다른 사용자가 정확한 코드로 검색하면 최소 프로필을 확인할 수 있다.
- 일부 문자만 입력하거나 대소문자를 바꾸면 다른 코드로 처리된다.

### AT-003 무료 제한

- 무료 사용자가 활성 루틴 4개를 만든다.
- 5번째를 만들거나 보관 항목을 재활성화하면 페이월 또는 정리 안내가 표시된다.
- 서버 직접 요청으로도 5번째 활성화가 거절된다.
- Premium 권한을 얻으면 5번째 활성화가 성공한다.

### AT-004 하루 시작 시각

- 시간대 `Asia/Seoul`, 시작 시각 `04:00`으로 설정한다.
- 03:59의 체크인은 전날 루틴 데이에 저장된다.
- 04:00의 체크인은 당일 루틴 데이에 저장된다.
- 시작 시각을 바꾸어도 과거 체크인은 이동하지 않는다.

### AT-005 스트릭

- 매일 예정된 항목을 3일 연속 완료하면 항목 스트릭이 3이 된다.
- 예정되지 않은 휴식일은 스트릭을 끊지 않는다.
- 예정된 날을 놓치면 다음 완료 시 새 스트릭이 시작된다.

### AT-006 오프라인

- 네트워크를 끄고 체크인한다.
- UI는 즉시 완료 상태를 보여준다.
- 앱을 재시작해도 상태가 유지된다.
- 네트워크 복구 후 서버에 한 건만 생성된다.

### AT-007 친구와 차단

- A가 B의 코드로 요청하고 B가 수락한다.
- A와 B는 친구 목록에 서로 보인다.
- B가 A를 차단하면 검색, 초대, 응원 전송이 차단된다.

### AT-008 구독 만료

- Premium 상태에서 활성 루틴 6개를 만든다.
- 구독이 만료된다.
- 6개 기록은 삭제되지 않고 체크인도 가능하다.
- 새 항목 추가 또는 보관 항목 재활성화는 4개 이하로 줄일 때까지 제한된다.

### AT-009 계정 삭제

- 사용자가 앱 내에서 삭제를 시작하고 확인한다.
- 인증 계정과 연결 데이터가 삭제 또는 정책에 따라 익명화된다.
- 삭제된 사용자의 코드는 검색되지 않는다.

---

## 21. 출시 전 미정이지만 개발을 막지 않는 항목

| 항목 | 개발 기본값 |
|---|---|
| 최종 앱 이름 | WeCanDay |
| iOS Bundle ID | `com.wecanday.app` 자리표시자 |
| Android Package | `com.wecanday.app` 자리표시자 |
| 첫 출시 언어 | 한국어, 영어 |
| 후속 언어 | 일본어, 스페인어, 중국어 간체·번체 후보 |
| 구독 가격 | 스토어에서 추후 설정, 코드 하드코딩 금지 |
| 무료 체험 | 기본 없음, RevenueCat Offering으로 추후 설정 |
| 광고 | 꺼짐 |
| 연령 기준 | 13+ 기본, 출시 지역 검토 후 조정 |
| 픽셀 에셋 | 임시 오리지널 플레이스홀더 후 교체 |

---

## 22. 공식 기술 참고 자료

- Expo Localization: https://docs.expo.dev/versions/latest/sdk/localization/
- Expo Router: https://docs.expo.dev/router/introduction/
- Expo Notifications: https://docs.expo.dev/versions/latest/sdk/notifications/
- Expo SecureStore: https://docs.expo.dev/versions/latest/sdk/securestore/
- Expo SQLite: https://docs.expo.dev/versions/latest/sdk/sqlite/
- Expo EAS Build: https://docs.expo.dev/build/introduction/
- i18next: https://www.i18next.com/
- Supabase Expo Social Auth: https://supabase.com/docs/guides/auth/quickstarts/with-expo-react-native-social-auth
- Supabase Row Level Security: https://supabase.com/docs/guides/database/postgres/row-level-security
- RevenueCat Expo: https://www.revenuecat.com/docs/getting-started/installation/expo
- Apple App Review Guidelines: https://developer.apple.com/app-store/review/guidelines/
- Google Play Billing: https://developer.android.com/google/play/billing
- Apple Account Deletion: https://developer.apple.com/support/offering-account-deletion-in-your-app/
- Google Play Account Deletion: https://support.google.com/googleplay/android-developer/answer/13327111

---

## 23. 최종 제품 방향

WeCanDay의 차별점은 단순히 체크박스와 스트릭을 제공하는 것이 아니다. 다음 세 요소를 하나의 경험으로 묶는 것이 핵심이다.

1. **사용자마다 다른 하루 경계**
2. **친구 코드 기반의 안전한 함께하기**
3. **오리지널 픽셀 동반자와 게임형 보상**

개발 과정에서 기능 수를 늘리는 것보다 이 세 가지가 정확하고 즐겁게 동작하도록 우선한다.
